type Todo
  @model
  @auth(
    rules: [
      { allow: owner } # normal users can only access their own todos
      { allow: groups, groups: ["Admin"] } # users in "Admin" group can access all todos
    ]
  ) {
  id: ID!
  name: String!
  description: String
  completed: Boolean!
  createdAt: AWSDateTime
}
